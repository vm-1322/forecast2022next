import Layout from 'components/Layout';
import HomePage from 'components/HomePage';

const Home: React.FC = () => {
  return (
    <Layout>
      <HomePage />
    </Layout>
  );
};

export default Home;
